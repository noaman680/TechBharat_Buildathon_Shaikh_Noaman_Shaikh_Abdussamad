// Shows the exact API payload MeetMind will send before the user confirms.
// This is a required HITL safety feature — see docs/BLUEPRINT.md §12.
// TODO: fetch via GET /api/integrations/{id}/preview/{item_id}.
export default function PayloadPreview() {
  return <div>TODO: payload preview (required before execute)</div>;
}
