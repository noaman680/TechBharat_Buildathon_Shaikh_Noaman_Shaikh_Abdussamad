"use client";
// File upload widget — posts to /api/meetings/upload and hands off to
// ProcessingProgress once a meeting_id comes back.
// TODO: implement drag-and-drop, call api.uploadMeeting(formData).
export default function MeetingUpload() {
  return (
    <div className="border-2 border-dashed rounded-lg p-8 text-center text-gray-500">
      TODO: drag-and-drop upload (audio/video/txt/vtt/srt)
    </div>
  );
}
