"use client";
// Live progress bar driven by subscribeToProgress() WebSocket events.
// TODO: render a progress bar + current stage label from the event stream.
export default function ProcessingProgress({ meetingId }: { meetingId: string }) {
  return <div>TODO: live progress for {meetingId}</div>;
}
