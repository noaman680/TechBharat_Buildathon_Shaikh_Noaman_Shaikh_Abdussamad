"use client";
// Core HITL surface: review/edit/reject action items, pick integrations per
// item, preview the exact outbound payload, then execute.
// TODO: implement per docs/BLUEPRINT.md §12 state machine.
export default function ApprovalDashboard({ approvalId }: { approvalId: string }) {
  return <div>TODO: approval dashboard for {approvalId}</div>;
}
