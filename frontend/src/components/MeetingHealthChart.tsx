"use client";
// Recharts-based visualization of talk-time balance, decision density,
// and action-item density — see compute_meeting_health() in the blueprint
// Appendix A.
export default function MeetingHealthChart() {
  return <div>TODO: meeting health chart (recharts)</div>;
}
