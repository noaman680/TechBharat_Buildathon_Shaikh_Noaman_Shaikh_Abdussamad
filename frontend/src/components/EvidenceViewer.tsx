"use client";
// Click an action item -> jump to the exact transcript timestamp that
// justified it. TODO: scroll transcript panel to evidence_timestamp.
export default function EvidenceViewer() {
  return <div>TODO: evidence viewer</div>;
}
