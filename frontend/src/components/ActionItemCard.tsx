// Single action item — title, owner, due date, confidence, evidence link.
// TODO: render fields from the ActionItem shape (see backend/app/agents/state.py).
export default function ActionItemCard() {
  return <div>TODO: action item card</div>;
}
