// Chronological list of every agent decision for a meeting, with reasoning
// and tool calls expandable per entry.
export default function AuditTimeline() {
  return <div>TODO: audit timeline</div>;
}
