// Structured report view — executive summary, decisions, risks, open questions.
// TODO: fetch via api.getReport(params.id).
export default function MeetingReportPage({ params }: { params: { id: string } }) {
  return <div className="p-8">TODO: structured report for {params.id}</div>;
}
