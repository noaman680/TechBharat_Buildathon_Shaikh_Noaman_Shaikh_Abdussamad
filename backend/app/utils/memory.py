"""Build the historical-context paragraph shown on the approval dashboard."""


def build_historical_context(related_meetings, overdue_items, carry_forwards) -> str:
    raise NotImplementedError(
        "TODO: summarize related past meetings, overdue commitments, and repeat "
        "commitments into a short paragraph for the approval UI"
    )
