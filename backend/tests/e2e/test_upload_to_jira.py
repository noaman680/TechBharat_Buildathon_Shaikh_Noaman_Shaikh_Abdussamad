"""E2E test placeholder — Playwright: full upload -> approve -> Jira ticket."""
import pytest

pytestmark = pytest.mark.skip(reason="TODO: needs Playwright + a running stack (docker compose up)")


async def test_full_pipeline_creates_jira_ticket():
    ...
