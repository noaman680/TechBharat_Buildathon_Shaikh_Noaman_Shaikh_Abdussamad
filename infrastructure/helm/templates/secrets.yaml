apiVersion: v1
kind: Secret
metadata:
  name: meetmind-secrets
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "{{ .Values.anthropicApiKey | default \"\" }}"
  OPENAI_API_KEY: "{{ .Values.openaiApiKey | default \"\" }}"
